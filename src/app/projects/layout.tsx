export const metadata = {
    title: 'Projects | Poorvi Bhaskar',
    description: 'Featured engineering projects in firmware, IoT, e-mobility, and embedded tooling with detailed case studies.',
};

export default function ProjectsLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
