export const metadata = {
    title: 'Technical Skills | Poorvi Bhaskar',
    description: 'Embedded systems skill stack — from hardware interfaces and RTOS to CAN protocols and modern firmware tooling.',
};

export default function SkillsLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
